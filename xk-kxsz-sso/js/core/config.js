config = {
    baseUrl: location.protocol + '//' + location.host,
    prefix: '/kxszApi/',
    domain: '.kxsz.com'
}
pages = {
    // index: config.baseUrl + '/#/'
    index: location.protocol + '//www' + config.domain
}