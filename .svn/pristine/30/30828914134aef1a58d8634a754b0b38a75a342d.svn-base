/* 关键字变色 */

function filterKeyWords (str, keyWords) {
    if (str) {
        str = str.replace(new RegExp(keyWords,'gm'), `<span style="color: red;">${keyWords}</span>`)
    }
    return str
}

export default filterKeyWords